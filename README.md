# 법률 문서 요약 및 상담 챗봇 프로젝트

ML + DL + LLM 융합 법률 서비스 플랫폼

---

## 프로젝트 개요

### 문제 인식
- 일반 사용자들은 계약서나 판례 같은 법률 문서를 읽고 이해하기 어려움
- 법률 용어는 어렵고, 문서 길이도 길어 핵심 내용 파악이 어려움

### 목표
- 법률 문서를 자동 분류 → 요약 → 질의응답 형태로 처리
- 사용자가 쉽게 이해하고 활용할 수 있는 챗봇 형태의 서비스 구현

---

## 주요 기능

| 기능 | 설명 |
|------|------|
| 사건 검색 시뮬레이션 | 사건 내용 입력 시 유사 판례 검색 및 승소 확률 예측 |
| RAG 기반 상담 챗봇 | 판례 데이터 기반 법률 질의응답 |
| LLM 키워드 추출 | GPT-4o-mini를 활용한 사건 도메인/쟁점 자동 분류 |
| PDF 업로드 지원 | 사용자 문서 업로드 후 RAG 기반 상담 가능 |
| 참조조문 설명 | LLM을 통한 법률 조문 쉬운 설명 제공 |

---

## 기술 스택

| 범주 | 사용 기술 |
|------|----------|
| Frontend | Streamlit |
| LLM | OpenAI GPT-4o-mini |
| Vector DB | FAISS |
| Embedding | OpenAI Embeddings |
| Framework | LangChain |
| NLP (폴백) | KoNLPy (Okt) |
| 데이터 처리 | Pandas |
| 컨테이너 | Docker |

---

## 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                      Streamlit UI                           │
├─────────────────────────────┬───────────────────────────────┤
│     탭1: 사건 검색 시뮬레이션    │     탭2: RAG 기반 상담 챗봇      │
├─────────────────────────────┴───────────────────────────────┤
│                                                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│  │ LLM 분류기  │    │  KoNLPy     │    │ Simple 폴백 │     │
│  │ (GPT-4o)   │ -> │  (Okt)      │ -> │ (키워드매칭) │     │
│  └─────────────┘    └─────────────┘    └─────────────┘     │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                  FAISS Vector DB                     │   │
│  │  - OpenAI Embeddings                                │   │
│  │  - 캐시 기반 재사용                                   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                  LangChain RAG                       │   │
│  │  - RunnableMap 파이프라인                            │   │
│  │  - 스트리밍 응답                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     Data Layer                              │
│  - 판례 데이터 (ZIP/CSV)                                    │
│  - 사건코드 매핑 테이블                                      │
│  - 임베딩/벡터스토어 캐시                                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 데이터 파이프라인

```
1. 데이터 수집
   법원 판례 데이터 (공공데이터 API)
              │
              ▼
2. 전처리 (preprocessing.py)
   - 판례 내용 파싱
   - LLM 기반 요약 생성
   - 당사자/쟁점/결과 추출
              │
              ▼
3. 벡터화 (law_page.py)
   - OpenAI Embeddings
   - FAISS 인덱싱
   - 캐시 저장
              │
              ▼
4. 서비스
   - 유사 판례 검색
   - RAG 기반 질의응답
```

---

## 화면 구성

### 탭1: 사건 검색 시뮬레이션
- 사용자 입장 선택 (원고/피고/검사/기타)
- 사건 내용 입력
- 유사 판례 검색 및 승소 확률 예측
- 참조조문 설명 제공

### 탭2: RAG 기반 상담 챗봇
- PDF 문서 업로드 (선택)
- 법률 관련 질문 입력
- 판례 기반 답변 생성
- 대화 히스토리 유지

---

## 설치 및 실행

자세한 설치 방법은 [SETUP.md](SETUP.md)를 참고하세요.

### 빠른 시작 (Docker)

```bash
# 1. 환경 변수 설정
cp .env.example .env
# .env 파일에서 OPENAI_API_KEY 설정

# 2. Docker 실행
cd docker
docker compose up -d

# 3. 브라우저 접속
# http://localhost:8501
```

---

## 데이터 출처

- 법원 판례정보: https://portal.scourt.go.kr/pgp/index.on?m=PGP1011M01&l=N&c=900
- 공공데이터 API: https://www.data.go.kr/data/15057123/openapi.do

---

## 팀 구성

| 역할 | 담당 |
|------|------|
| 데이터 수집/전처리 | - |
| 모델 개발 | - |
| 프론트엔드 | - |
| 배포/인프라 | - |

---

## 라이선스

본 프로젝트는 학습 목적으로 제작되었습니다.

---

## 주의사항

본 서비스는 참고용이며, 실제 법률 자문이 아닙니다.
정확한 법률 상담은 전문 변호사에게 문의하세요.
